// import { iAction } from '../interfaces/reduxInterfaces';
// import { iResourceListItem } from '../interfaces/externalData interfaces/externalData.interfaces';
// import { iSpellItem } from '../interfaces/externalData interfaces/spells.interface';
// // import { iSpellItem } from '../interfaces/externalData interfaces/spells.interface';

// const initialState: {spells: iResourceListItem[], spell: iSpellItem | null} = { spells: [], spell: null };

// const spellsStateReducer = (
//   state: iResourceListItem[] = initialState,
//   { type, payload }: iAction<iResourceListItem>,
// ) => {
//   switch (type) {
//     case 'POPULATE_SPELLS':
//       return {
//         ...state,
//         spells: payload,
//       };
//     default:
//       return state;
//   }
// };

// export default spellsStateReducer;
export {};
